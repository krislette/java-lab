package list;

public class Crud {
    
    /******************************** HERE ARE THE CRUD NOTES ********************************/
    // Create   ->  arrayIdentifier.add(Instantiate);
    // Read     ->  arrayIdentifer.get(elementIndex); | [Requires a CATCHER variable]
    // Update   ->  arrayIdentifier.set(elementIndex, (elementValue or Instantiate));
    // Delete   ->  | DELETE Specific Index                   | CLEAR ArrayList             |
    //              |-----------------------------------------------------------------------|
    //              | arrayIdentifier.remove(elementIndex)    | arrayIdentifier.clear();    |
    
    /**************************** ADDITIONAL ARRAY LIST OPERATIONS ***************************/
    // Size     ->  arrayIdentifier.size();
    // Traverse ->  for each loop or for loop
}